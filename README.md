# lang_rust
 record of learning rust languages according to "rust programming official guide" by Steve and Carol

# building projects
 해당 Repository는 가장 기본적인 "cargo" 명령어로 생산되는 프로젝트들의 목록(폴더별)들로 이루어져있습니다.
 local 로 가지고 가신 후에는 개별 폴더에서 project를 빌드 후에 보시기 바랍니다.
 
 [명령어] 
 - cargo build
